module.exports = ({ strapi }) => ({
  async bootstrap() {
    // Temporarily disabled complex bootstrap logic to debug startup issues
    console.log('Strapi bootstrap completed (simplified)');
  },
});  